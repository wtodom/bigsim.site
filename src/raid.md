<Form>
    <div key={`raid-checkbox`} className="mb-3">
    <Form.Check // prettier-ignore
        type='checkbox'
        id={`raid-checkbox`}
        label={`Amirdrassil, the Dream's Hope`}
    />
    </div>
    <div key={`mplus-checkbox`} className="mb-3">
    <Form.Check // prettier-ignore
        type='checkbox'
        id={`mplus-checkbox`}
        label={`Mythic Plus`}
    />
    </div>
    <div key={`crafted-checkbox`} className="mb-3">
    <Form.Check // prettier-ignore
        type='checkbox'
        id={`crafted-checkbox`}
        label={`Crafted`}
    />
    </div>
</Form>
